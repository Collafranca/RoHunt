---
import AppShell from "../layouts/AppShell.astro";
import { getSavedJobsPageData } from "../lib/api-client";
import { renderJobsState } from "../components/jobs/JobsSurface";

const data = await getSavedJobsPageData();
const state = renderJobsState(data);
---

<AppShell>
  <section>
    <h1>Saved Jobs</h1>
    <div data-state={state.kind}>
      {
        state.kind === "data" ? (
          <>
            <ul>
              {state.items.map((_, index) => <li>Saved job {index + 1}</li>)}
            </ul>
            <p>{state.message}</p>
          </>
        ) : (
          <p>{state.message}</p>
        )
      }
    </div>
  </section>
</AppShell>
