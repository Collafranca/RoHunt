---
import AppShell from "../layouts/AppShell.astro";
import { getHomePageData } from "../lib/api-client";
import { renderHomeState } from "../components/analytics/AnalyticsSurface";

const data = await getHomePageData();
const state = renderHomeState(data);
---

<AppShell>
  <section>
    <h1>RoHunt</h1>
    <div data-state={state.kind}>
      {
        state.kind === "data" ? (
          <>
            <ul>
              {state.items.map((_, index) => <li>Platform item {index + 1}</li>)}
            </ul>
            <p>{state.message}</p>
          </>
        ) : (
          <p>{state.message}</p>
        )
      }
    </div>
  </section>
</AppShell>
